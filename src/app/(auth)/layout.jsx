import React from 'react';

export const metadata = {
    title: "Sun Cart | Authentication",
    description: "Sun-Cart homepage",
};

const AuthLayout = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default AuthLayout;